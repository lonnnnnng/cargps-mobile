# 使用系统 LocationManager 作为唯一定位主来源

CarGPS 手机版采用 Android `LocationManager` 的 `GPS_PROVIDER` 作为坐标、速度和行程统计的唯一主来源，并用 NMEA 与 `GnssStatus` 补充诊断信息。拆分阶段保留这条离线定位路径可以避免迁移同时改变统计时序；代价是应用必须自行处理样本质量、速度平滑、定位过期和后台记录生命周期，且不得让 NMEA 形成第二套独立统计时序。后续若评估融合定位，应作为手机版独立架构变更验证，不能直接替换当前统计主来源。
